name: proyecto_final_dam
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_slidable: ^3.0.1
  google_fonts: ^6.1.0
  http: ^1.2.1
  flutter_secure_storage: ^9.0.0
  animate_do: ^3.3.4
  sqflite: ^2.3.3
  connectivity_plus: ^6.0.2

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true